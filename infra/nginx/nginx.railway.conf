# AUTO-GENERATED by scripts/gen-railway-configs.mjs from
# infra/nginx/nginx-with-site.conf — do not edit by hand. Re-run the
# generator whenever the source nginx config changes:
#
#   node scripts/gen-railway-configs.mjs
#
# What's different from the source:
#   1. Every `proxy_pass http://<service>:<port>` whose hostname matches
#      a service in scripts/deploy/shared/services.mjs is rewritten to
#      `proxy_pass http://<service>.railway.internal:<port>` so the
#      ingress resolves backends through Railway's private DNS instead
#      of the docker-compose bridge network.
#   2. For Bam app services (api, helpdesk-api, mcp-server, …), the
#      upstream port is rewritten to 8080 because Railway sets PORT=8080
#      on every container and the services bind to that, NOT to their
#      docker-compose nominal port (4000, 4001, 3001, …).
#   3. Third-party services (minio, qdrant, livekit) keep their original
#      ports because they ignore $PORT and bind to hardcoded defaults.
#   4. The `listen N;` directive is rewritten to `listen 8080;` so the
#      frontend container binds to Railway's assigned PORT and its own
#      healthcheck passes.
#   5. A `resolver __RESOLVER__ valid=10s;` directive is added to the
#      server block. The entrypoint script replaces __RESOLVER__ with
#      the nameservers from /etc/resolv.conf so nginx resolves upstream
#      hostnames through Railway's DNS at request time (10s TTL)
#      instead of caching the startup IP forever. Without this, any
#      upstream redeploy gives the service a new internal IP and the
#      frontend 502s at that route until it is also redeployed.
#   6. Each proxy_pass upstream hostname is moved into a `set` variable
#      followed by an explicit `rewrite` that reproduces the original
#      URI substitution. nginx only consults the resolver when the
#      proxy_pass URL contains a variable, so a hostname without the
#      `$` indirection would still be statically resolved despite the
#      resolver directive. Trust the generator for these transforms;
#      edit infra/nginx/nginx-with-site.conf (the source) for any real
#      config change.
#
server {
    listen 8080;
    resolver __RESOLVER__ valid=10s;
    server_name _;
    client_max_body_size 25m;

    # ── Gzip ──────────────────────────────────────────────────────
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml image/svg+xml font/woff2;

    # ── Security Headers ─────────────────────────────────────────
    add_header X-Content-Type-Options nosniff always;
    add_header X-Frame-Options DENY always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

    # ── BBB Frontend SPA ──────────────────────────────────────────
    location /b3/ {
        alias /usr/share/nginx/html/b3/;
        try_files $uri $uri/ /b3/index.html;
    }

    # ── BBB API proxy ─────────────────────────────────────────────
    location /b3/api/ {
        set $rw_upstream_1 "api.railway.internal";
        rewrite ^/b3/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── BBB WebSocket ─────────────────────────────────────────────
    location /b3/ws {
        set $rw_upstream_2 "api.railway.internal";
        rewrite ^/b3/ws(.*)$ /ws$1 break;
        proxy_pass http://$rw_upstream_2:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400s;
    }

    # ── Helpdesk Frontend SPA ─────────────────────────────────────
    # D-010: path-based multi-tenant routing ("/helpdesk/<org>/<project>/...")
    # is handled entirely by the SPA router. Deep links like
    # /helpdesk/mage-inc/tickets/42 do not exist as real files, so the
    # try_files fallback must resolve to /helpdesk/index.html which boots
    # the SPA and its parseTenantFromPath() takes over. The /helpdesk/api/
    # and /helpdesk/ws locations below take longest-prefix precedence and
    # are unaffected by this fallback.
    location /helpdesk/ {
        alias /usr/share/nginx/html/helpdesk/;
        try_files $uri $uri/ /helpdesk/index.html;
    }

    # ── Helpdesk API proxy ────────────────────────────────────────
    location /helpdesk/api/ {
        set $rw_upstream_3 "helpdesk-api.railway.internal";
        rewrite ^/helpdesk/api/(.*)$ /helpdesk/$1 break;
        proxy_pass http://$rw_upstream_3:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── Helpdesk WebSocket ────────────────────────────────────────
    location /helpdesk/ws {
        set $rw_upstream_4 "helpdesk-api.railway.internal";
        rewrite ^/helpdesk/ws(.*)$ /helpdesk/ws$1 break;
        proxy_pass http://$rw_upstream_4:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400s;
    }

    # ── Banter Frontend SPA ─────────────────────────────────────────
    location /banter/ {
        alias /usr/share/nginx/html/banter/;
        try_files $uri $uri/ /banter/index.html;
    }

    # ── Banter API proxy ──────────────────────────────────────────
    location /banter/api/ {
        set $rw_upstream_5 "banter-api.railway.internal";
        rewrite ^/banter/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_5:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Banter WebSocket ──────────────────────────────────────────
    location /banter/ws {
        set $rw_upstream_6 "banter-api.railway.internal";
        rewrite ^/banter/ws(.*)$ /ws$1 break;
        proxy_pass http://$rw_upstream_6:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400s;
    }

    # ── Beacon Frontend SPA ──────────────────────────────────────────
    location /beacon/ {
        alias /usr/share/nginx/html/beacon/;
        try_files $uri $uri/ /beacon/index.html;
    }

    # ── Beacon API proxy ────────────────────────────────────────────
    location /beacon/api/ {
        set $rw_upstream_7 "beacon-api.railway.internal";
        rewrite ^/beacon/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_7:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Brief Frontend SPA ──────────────────────────────────────────
    location /brief/ {
        alias /usr/share/nginx/html/brief/;
        try_files $uri $uri/ /brief/index.html;
    }

    # ── Brief API proxy ──────────────────────────────────────────────
    location /brief/api/ {
        set $rw_upstream_8 "brief-api.railway.internal";
        rewrite ^/brief/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_8:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── Brief WebSocket (Yjs collaboration) ──────────────────────────
    location /brief/ws {
        set $rw_upstream_9 "brief-api.railway.internal";
        rewrite ^/brief/ws(.*)$ /ws$1 break;
        proxy_pass http://$rw_upstream_9:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ── Bolt Frontend SPA ───────────────────────────────────────────
    location /bolt/ {
        alias /usr/share/nginx/html/bolt/;
        try_files $uri $uri/ /bolt/index.html;
    }

    # ── Bolt API proxy ──────────────────────────────────────────────
    location /bolt/api/ {
        set $rw_upstream_10 "bolt-api.railway.internal";
        rewrite ^/bolt/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_10:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Bearing SPA ─────────────────────────────────────────────────
    location /bearing/ {
        alias /usr/share/nginx/html/bearing/;
        try_files $uri $uri/ /bearing/index.html;
    }

    # ── Bearing API proxy ────────────────────────────────────────────
    location /bearing/api/ {
        set $rw_upstream_11 "bearing-api.railway.internal";
        rewrite ^/bearing/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_11:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Board SPA ────────────────────────────────────────────────────
    location /board/ {
        alias /usr/share/nginx/html/board/;
        try_files $uri $uri/ /board/index.html;
    }

    # ── Board API proxy ──────────────────────────────────────────────
    location /board/api/ {
        set $rw_upstream_12 "board-api.railway.internal";
        rewrite ^/board/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_12:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ── Board WebSocket (tldraw collaboration + chat) ────────────────
    location /board/ws {
        set $rw_upstream_13 "board-api.railway.internal";
        rewrite ^/board/ws(.*)$ /ws$1 break;
        proxy_pass http://$rw_upstream_13:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ── Blast Frontend SPA ────────────────────────────────────────
    location /blast/ {
        alias /usr/share/nginx/html/blast/;
        try_files $uri $uri/ /blast/index.html;
    }

    # ── Blast API proxy ─────────────────────────────────────────────
    location /blast/api/ {
        set $rw_upstream_14 "blast-api.railway.internal";
        rewrite ^/blast/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_14:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Blast tracking endpoints (short paths for email links) ─────
    location /t/ {
        set $rw_upstream_15 "blast-api.railway.internal";
        rewrite ^/t/(.*)$ /t/$1 break;
        proxy_pass http://$rw_upstream_15:8080;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /unsub/ {
        set $rw_upstream_16 "blast-api.railway.internal";
        rewrite ^/unsub/(.*)$ /unsub/$1 break;
        proxy_pass http://$rw_upstream_16:8080;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ── Bond Frontend SPA ─────────────────────────────────────────
    location /bond/ {
        alias /usr/share/nginx/html/bond/;
        try_files $uri $uri/ /bond/index.html;
    }

    # ── Bond API proxy ──────────────────────────────────────────────
    location /bond/api/ {
        set $rw_upstream_17 "bond-api.railway.internal";
        rewrite ^/bond/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_17:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Bench Frontend SPA ────────────────────────────────────────
    location /bench/ {
        alias /usr/share/nginx/html/bench/;
        try_files $uri $uri/ /bench/index.html;
    }

    # ── Bench API proxy ─────────────────────────────────────────────
    location /bench/api/ {
        set $rw_upstream_18 "bench-api.railway.internal";
        rewrite ^/bench/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_18:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Blank Frontend SPA ─────────────────────────────────────────
    location /blank/ {
        alias /usr/share/nginx/html/blank/;
        try_files $uri $uri/ /blank/index.html;
    }

    # ── Blank API proxy ──────────────────────────────────────────────
    location /blank/api/ {
        set $rw_upstream_19 "blank-api.railway.internal";
        rewrite ^/blank/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_19:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Public form endpoints (no auth) ──────────────────────────────
    location /forms/ {
        set $rw_upstream_20 "blank-api.railway.internal";
        rewrite ^/forms/(.*)$ /forms/$1 break;
        proxy_pass http://$rw_upstream_20:8080;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # ── Bill Frontend SPA ───────────────────────────────────────────
    location /bill/ {
        alias /usr/share/nginx/html/bill/;
        try_files $uri $uri/ /bill/index.html;
    }

    # ── Bill API proxy ───────────────────────────────────────────
    location /bill/api/ {
        set $rw_upstream_21 "bill-api.railway.internal";
        rewrite ^/bill/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_21:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Bill public invoice pages ────────────────────────────────
    location /invoice/ {
        set $rw_upstream_22 "bill-api.railway.internal";
        rewrite ^/invoice/(.*)$ /invoice/$1 break;
        proxy_pass http://$rw_upstream_22:8080;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # ── Book Frontend SPA ──────────────────────────────────────────
    location /book/ {
        alias /usr/share/nginx/html/book/;
        try_files $uri $uri/ /book/index.html;
    }

    # ── Book API proxy ───────────────────────────────────────────
    location /book/api/ {
        set $rw_upstream_23 "book-api.railway.internal";
        rewrite ^/book/api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_23:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    # ── Book public booking pages ────────────────────────────────
    location /meet/ {
        set $rw_upstream_24 "book-api.railway.internal";
        rewrite ^/meet/(.*)$ /meet/$1 break;
        proxy_pass http://$rw_upstream_24:8080;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # ── File serving (shared, proxied through BBB API) ────────────
    location /files/ {
        set $rw_upstream_25 "api.railway.internal";
        rewrite ^/files/(.*)$ /files/$1 break;
        proxy_pass http://$rw_upstream_25:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # ── MCP server ────────────────────────────────────────────────
    location /mcp/ {
        set $rw_upstream_26 "mcp-server.railway.internal";
        rewrite ^/mcp/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_26:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # ── BBB helpdesk-api proxy (for BBB frontend's helpdesk panel) ─
    location /helpdesk-api/ {
        set $rw_upstream_27 "helpdesk-api.railway.internal";
        rewrite ^/helpdesk-api/(.*)$ /$1 break;
        proxy_pass http://$rw_upstream_27:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # ── Documentation static files (in-app Help viewer) ───────────
    # Serves docs/apps/*/guide.md and related assets. The HelpViewer
    # component in each frontend app fetches /docs/apps/{slug}/guide.md.
    location /docs/ {
        alias /usr/share/nginx/html/docs/;
        try_files $uri $uri/ =404;
        add_header Cache-Control "public, max-age=3600";
    }

    # ── Static asset caching (exclude API/proxy paths) ────────────
    location ~* ^/(b3|helpdesk|banter|beacon|brief|bolt|bearing|board|bond|blast|blank|book|bench)/assets/.*\.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # ── Marketing site (root domain) ──────────────────────────────
    # Everything not claimed by one of the prefix locations above falls
    # through to the site service. Prefix matches (/b3/, /helpdesk/,
    # /banter/, /files/, /mcp/, /helpdesk-api/) take precedence over
    # this plain `/` prefix per nginx's longest-prefix-wins rule.
    #
    # The marketing site is intentionally embeddable in iframes from any
    # origin so press-release viewers and partner pages can include it.
    # nginx's add_header does not inherit from the server block once a
    # location declares its own, so the other security headers must be
    # repeated here. X-Frame-Options is omitted and CSP frame-ancestors
    # is set to * to override the server-level DENY.
    location / {
        add_header X-Content-Type-Options nosniff always;
        add_header Referrer-Policy strict-origin-when-cross-origin always;
        add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
        add_header Content-Security-Policy "frame-ancestors *" always;
        set $rw_upstream_28 "site.railway.internal";
        proxy_pass http://$rw_upstream_28:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
