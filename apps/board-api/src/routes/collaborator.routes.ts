import type { FastifyInstance } from 'fastify';

export default async function collaboratorRoutes(_fastify: FastifyInstance) {
  // Board collaborator management routes — placeholder, to be implemented
}
